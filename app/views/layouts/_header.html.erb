<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "BasedChef", root_path, class: "navbar-brand" %>
      </div>
      <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <li class="active"><%= link_to "Home", root_path %> </li>
          <li><%= link_to "About", about_path %></li>
          <% if signed_in? %>
          <li><%= link_to "Dishes", dishes_path, onclick: "ga('send', 'event', 'header_dishes', 'click');" %></li>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <li><%= link_to "Profile", current_user %></li>
            <li><%= link_to "Settings", profile_user_path(current_user) %></li>
            <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
      <!--  <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= image_tag current_user.avatar(:thumb), width: "20", height: "20" %>
                <%= current_user.name %>
              <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", profile_user_path(current_user) %></li>
                <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
              </ul>
            </li>
 -->
          <% else %>

            <li><%= link_to "Add a Dish", "/auth/facebook", id: "sign_in", onclick: "ga('send', 'event', 'header_add_dish', 'click');" %></li>
             
          <% end %>
        </ul>
      </div><!-- .navbar-collapse -->
  </div><!-- .container -->
</div>

<%= feedback_tab(:position => 'right') %>




