<!--Search for chefs by city, style, or name.
<%= form_tag(users_path, :method => 'get') do %>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
<% end %>
-->

<!-- Jumbotron -->
<div id="spotlight" class="jumbotron">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <h1>Share and sell what you cook at home.</h1>
        <ul class="list-unstyled lead">
            <li class="clearfix">1. Cook delicious food and create a menu of dishes.</li>
            <li class="clearfix">2. Place your menu items up for sale (if you wish).</li>
            <li class="clearfix">3. Share dishes with friends and view other's dishes.</li>
          </ul>
      </div>
    </div>
    <% if signed_in? %>
      <%= link_to "Go to my profile", current_user, class: "btn btn-primary btn-lg" %>
    <% else %>
      <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in", class: "btn btn-primary btn-lg", onclick: "ga('send', 'event', 'fb_signin', 'click');" %>
    <% end %>
  </div>
</div>
<%#= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>




  <script type="text/javascript">
    $(document).ready(function() { 

      $.backstretch('/assets/jumbo-bg.jpg');

    //   // Feedback Tab
    //   $('.feedback_link').feedback( {
    //       tabPosition: 'right'
    //   }); 
    
    });
  </script>